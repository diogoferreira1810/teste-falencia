import styled from 'styled-components';

const NavbarContainer = styled.nav`
  width: 100%;
    background: #333;
  padding: 0.5rem 0;
  position: fixed;
  top: 0;
  left: 0;
  z-index: 1000;
`;

export default NavbarContainer;